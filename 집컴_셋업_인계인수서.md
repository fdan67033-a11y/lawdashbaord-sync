# 🏠 집 PC 셋업 인계인수서 (git 자동 동기화 방식)

> **목표**: 집컴에서 법령대시보드를 실행하고, **회사컴과 회독·노트·설명을 자동 동기화**한다.
> **방식(중요)**: 각 PC가 **자기 로컬로 대시보드를 실행**하고, 회독·노트는 **git으로 자동 동기화**한다.
> **터널·중앙서버 24시간 켜둘 필요 없음.** (터널 방식 `집컴_동기화서버_인계인수서.md`는 "폰에서 실시간 접속"이 필요할 때만 참고)
> **예상 소요**: 최초 1회 20~40분(데이터 복사 시간 포함).

---

## 0. 전체 그림

```
[회사컴]  로컬 대시보드 + dashboard.db ──┐
                                          ├── GitHub repo (코드 + 회독·노트 JSON)
[집컴]    로컬 대시보드 + dashboard.db ──┘
   * 회독·노트 = data/sync/user_data.json 으로 git 동기화 (킬 때 pull / 닫을 때 push)
   * 코드/UI  = git pull/push
   * 법제처 API 호출은 각자 PC에서 → 집컴 공인IP도 법제처에 등록돼 있어야 함
```

**핵심 원칙**
1. 대시보드는 **집컴 로컬**로 실행(회사와 별개 인스턴스).
2. **회독·노트는 git 자동 동기화**(사이드바 `🔄 동기화` 토글).
3. **단일 사용자 규율**: 한 곳에서 쓰고 닫을 때 push → 다른 곳은 열 때 pull. **동시에 두 곳 편집 금지**(나중에 push한 쪽이 이김).

---

## 1. 사전 확인 (집컴)
- [ ] `git --version` 됨 (없으면 https://git-scm.com 설치)
- [ ] `py --version` 됨 (Python)
- [ ] 인터넷 됨
- [ ] **법제처 API에 집컴 공인 IP 등록됨** (내 아이피 검색해서 확인). 안 돼 있으면 API "불러오기 실패" 남 → 법제처 오픈API 신청정보에서 집 IP 등록.

---

## 2. 저장소 클론

원하는 폴더에서:
```bat
git clone https://github.com/fdan67033-a11y/lawdashbaord-sync.git
cd lawdashbaord-sync
```
→ 코드·UI·실행스크립트 + 소형 참조데이터(예규·풀·한자맵) + **회사에서 만든 회독·노트(data/sync/user_data.json)** 까지 다 받아집니다.
필요한 파이썬 패키지:
```bat
py -m pip install flask requests python-dotenv
```

---

## 3. API 인증키(.env) 설정
```bat
copy .env.example .env
```
→ `.env` 파일을 열어 `OPENLAW_OC` 를 **본인 OC 키**로 채우기:
```
OPENLAW_OC=sp-law-study
```
(회사 .env 와 같은 값. `.env` 는 git에 안 올라가니 집에서 직접 만들어야 함.)

---

## 4. 대용량 데이터 전송 (git엔 없음 — USB/클라우드로 복사)

clone엔 코드만 있고, 아래 대용량 DB는 없습니다. **회사컴에서 복사해 오세요.**

| 파일 | 크기 | 필요도 | 없으면 |
|---|---|---|---|
| **`data/prec_index.db`** | 72M | **권장(전체기능)** | 판례·올타(질의·상담)·조문연결 배지가 안 뜸 |
| `data/cache/` 폴더 | ~290M | 선택 | 없어도 API가 다시 받음(처음에 느림). 복사하면 빠름 |
| `data/olta_qa.db` | 443M | 불필요 | 대시보드는 안 씀(리딩팩 재생성 스크립트 전용) |

→ **최소 `data/prec_index.db` 하나만** 회사에서 복사해 집 `data\` 폴더에 넣으면 됩니다.
(회독·노트가 들어있는 `dashboard.db`는 복사 안 해도 됩니다 — 아래 6절 첫 동기화로 자동 생성·채워짐.)

---

## 5. 첫 실행 + (선택)WAL
```bat
법제처대시보드_시작.bat  (더블클릭)
```
→ http://localhost:6155 열림. 처음 실행 시 `dashboard.db` 가 자동 생성됩니다.

(선택) 동시접속 안정화 — 서버 끈 상태에서 한 줄:
```bat
py -c "import sqlite3,os; c=sqlite3.connect(os.path.join('data','dashboard.db')); print(c.execute('PRAGMA journal_mode=WAL').fetchone()[0]); c.close()"
```

---

## 6. 첫 동기화 — 회사 회독·노트 받아오기

1. 대시보드 왼쪽 사이드바 **`🔄 동기화`** 펼치기
2. **`🔄 지금 동기화`** 클릭 → git pull 되면서 `user_data.json` → DB 로 들어옴
   → 회사에서 단 **회독·노트·설명이 집컴에 그대로** 나타남 ✅
3. **`자동 동기화` 체크** → 이제부터 **킬 때 pull / 닫을 때 push** 자동

> 처음 켰을 때 회독·노트가 안 보이면 `🔄 지금 동기화` 한 번 눌러주세요(브라우저 새로고침).

---

## 7. 일상 사용 흐름

| 시점 | 동작(자동 동기화 ON) |
|---|---|
| 집에서 앱 킬 때 | 자동 `git pull` → 회사서 바뀐 회독·노트 반영 |
| 집에서 앱 닫을 때 | 자동 `git push` → 내가 한 것 회사로 |
| 언제든 수동 | `🔄 지금 동기화` 버튼 |

⚠️ **닫을 때 push 주의**: 서버 콘솔 창을 **X로 강제 종료하면 push가 안 될 수 있음**.
→ **닫기 전에 `🔄 지금 동기화`를 누르거나**, 콘솔 창에서 **Ctrl+C 로 종료**하세요. (그게 가장 확실)

---

## 8. 회사에서 개발한 코드 받기 (코드 업데이트)

회사에서 기능을 추가/수정하고 push했다면, 집에서:
```bat
git pull
```
→ 최신 코드·UI 반영. (대시보드 재시작하면 적용)
- 자동 동기화가 ON이면 **앱 킬 때 git pull에 코드까지 같이** 따라옵니다.
- 집에서 코드를 고쳤다면 `git add -A && git commit -m "..." && git push` 로 회사와 공유.

---

## 9. 문제 해결 (FAQ)

| 증상 | 확인 |
|---|---|
| "불러오기 실패"(검색·조회) | `.env`의 OPENLAW_OC 채웠나 / 집 공인IP 법제처 등록됐나 |
| 회독·노트가 회사와 다름 | `🔄 지금 동기화` 눌러 pull. 동시에 두 곳 편집했는지 확인 |
| 판례·올타 배지가 안 뜸 | `data/prec_index.db` 를 회사서 복사해 넣었나(4절) |
| push가 안 됨 | 콘솔에 git 오류 확인. 처음이면 GitHub 로그인 창(브라우저) 떴는지 |
| 동기화 충돌(rebase 오류) | 콘솔에서 `git status` → 막히면 Claude에게 "동기화 충돌났어" |

---

## 10. 체크리스트
- [ ] git·python 설치, 집 IP 법제처 등록 (1)
- [ ] `git clone` + pip install (2)
- [ ] `.env` 만들고 OC 채우기 (3)
- [ ] `data/prec_index.db` 복사 (4)
- [ ] `법제처대시보드_시작.bat` 실행 → localhost:6155 (5)
- [ ] `🔄 지금 동기화` → 회독·노트 받아지는지 확인 (6)
- [ ] `자동 동기화` 체크 (6)
- [ ] 닫기 전 동기화 습관화 (7)

---
막히면 이 채팅에서 **"인계인수서(집컴셋업) O절에서 막혔어"** 라고 하면 이어서 도와드립니다.

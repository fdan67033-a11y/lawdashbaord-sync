# 📋 집 PC 동기화 서버 구축 인계인수서

> **목표**: 집컴을 "중앙 서버"로 만들어 **회사컴·집컴·폰**이 대시보드의 **회독·노트·설명**을 하나의 데이터로 동기화한다.
> **원리**: 집컴에서 [대시보드(Flask, 6155)] + [Cloudflare Tunnel]을 돌리고, 회사컴·폰은 그 **공개 https 주소에 브라우저로 접속**만 한다. DB가 하나뿐이라 자동 동기화된다(구글 캘린더와 같은 구조).
> **예상 소요**: 최초 1회 20~30분. 이후엔 `.bat` 더블클릭 한 번.

---

## 0. 전체 그림

```
            ┌─ 회사컴 (브라우저만) ─┐
집컴 [대시보드 + dashboard.db 1개] ─┼─ 집컴 (브라우저) ────┤  → 셋 다 같은 DB
   └ Cloudflare Tunnel(공개주소)    └─ 폰 (브라우저) ──────┘

  * 법제처 API 호출은 언제나 "집컴"에서 나감 → 집컴 IP만 등록돼 있으면 됨.
```

핵심 3가지:
1. **대시보드는 집컴 한 곳에서만** 실행 (회사컴에선 로컬 실행 X, 브라우저로 접속만)
2. **dashboard.db는 집컴 것 하나만** 정본으로 사용
3. **Cloudflare Tunnel**이 집컴을 안전하게 외부에 노출 (포트포워딩·고정IP 불필요)

---

## 1. 사전 확인 (집컴)

- [ ] 대시보드 프로젝트 폴더 존재: `C:\todo_manual_dashboard\law_dashboard_work`
- [ ] Python 설치됨 (명령창에서 `py --version` 확인)
- [ ] 인터넷 연결됨
- [ ] **법제처 API에 "집컴 공인 IP"가 등록돼 있음** (OC 키: `.env의 OPENLAW_OC`)
  - 집컴 현재 공인 IP 확인: 브라우저에서 `https://ipconfig.kr` 또는 검색창에 "내 아이피"
  - 그 IP가 법제처 오픈API 신청정보의 등록 IP와 같아야 API가 됩니다.
  - ⚠️ 집 IP가 유동이면 나중에 바뀔 수 있음 → 안 되면 재등록 (7절 참고)

---

## 2. ⚠️ 데이터 정본 결정 (제일 중요)

지금은 **회사컴 dashboard.db**와 **집컴 dashboard.db**가 **따로** 있습니다(회독·노트가 각각 쌓여 있음).
앞으로는 **집컴 것 하나만** 씁니다. 그래서 먼저 정하세요:

- **(권장) 집컴 DB를 정본으로 그냥 사용** → 회사컴에 있던 기존 회독·노트는 집컴엔 없음.
- **회사컴에 쌓인 회독·노트도 살리고 싶다** → 두 DB **병합**이 필요. 이건 덮어쓰기 위험이 있으니 **Claude에게 "dashboard.db 두 개 병합해줘"** 라고 부탁해서 함께 진행하세요. (회사컴 `data\dashboard.db`를 집컴으로 복사해와서 병합)

> 지금 이 문서대로 세팅하면 **이후의 모든 회독·노트·설명은 집컴 DB에 통합**되어 세 기기에서 같이 보입니다.

**백업 먼저** (안전):
```
copy "C:\todo_manual_dashboard\law_dashboard_work\data\dashboard.db" "C:\todo_manual_dashboard\law_dashboard_work\data\dashboard_backup.db"
```

---

## 3. WAL 모드 적용 (동시 접속 안정화) — 1회만

여러 기기가 동시에 읽고 쓸 때 잠금 충돌을 줄여줍니다. **대시보드가 꺼진 상태**에서 명령창(cmd)에 한 줄:

```
py -c "import sqlite3; c=sqlite3.connect(r'C:\todo_manual_dashboard\law_dashboard_work\data\dashboard.db'); print('journal_mode =', c.execute('PRAGMA journal_mode=WAL').fetchone()[0]); c.close()"
```

→ `journal_mode = wal` 이 출력되면 성공 (한 번만 하면 계속 유지됨).

### (권장) 멀티 접속 부드럽게 — threaded 켜기
`app_law_notes_v20.pyw` 파일의 **맨 끝부분**(약 4608줄)을 찾아:
```python
app.run(host="127.0.0.1", port=6155, debug=False)
```
→ 아래처럼 `threaded=True` 추가 후 저장:
```python
app.run(host="127.0.0.1", port=6155, debug=False, threaded=True)
```
(여러 기기가 동시에 눌러도 요청이 밀리지 않고 처리됩니다. 안 해도 동작은 하지만 권장.)

---

## 4. Cloudflare Tunnel 설치

둘 중 하나:

### 방법 A — winget (권장, 간단)
관리자 명령창(cmd)에서:
```
winget install --id Cloudflare.cloudflared
```
설치 후 새 명령창에서 `cloudflared --version` 이 나오면 성공.

### 방법 B — exe 직접 다운로드
1. https://github.com/cloudflare/cloudflared/releases 접속
2. `cloudflared-windows-amd64.exe` 다운로드
3. 파일명을 **`cloudflared.exe`** 로 바꿔서 **프로젝트 폴더**(`C:\todo_manual_dashboard\law_dashboard_work`)에 넣기
   → 이렇게 하면 `동기화서버_시작.bat`이 그대로 인식합니다.

---

## 5. 실행 (오늘의 목표) — 원클릭

프로젝트 폴더의 **`동기화서버_시작.bat`** 더블클릭.

1. 대시보드 서버 창이 하나 뜸(포트 6155)
2. 7초 뒤 터널이 시작되고, 화면에 이런 주소가 뜹니다:
   ```
   https://random-words-1234.trycloudflare.com
   ```
3. **이 주소를 폰 브라우저에 입력** → 대시보드가 폰에서 열림 🎉
4. 회사컴에서도 같은 주소를 브라우저에 입력하면 동일하게 열림

### 동기화 테스트
- 폰에서 아무 법령 열고 **회독 +1**
- 집컴 브라우저에서 같은 조문 새로고침 → **회독 반영됨** → 동기화 성공 ✅

> ⚠️ **Quick Tunnel 주소는 껐다 켤 때마다 바뀝니다.** 매번 새 주소를 확인해야 함. 고정 주소를 원하면 6절.

---

## 6. (선택) 고정 주소 + 로그인 잠금 — 오래 쓸 거면

Quick Tunnel은 주소가 매번 바뀌고 **주소만 알면 아무나 접속**됩니다(노트가 개인자료라 위험). 제대로 쓰려면:

### 6-1. 고정 주소 (Named Tunnel) — Cloudflare 무료 계정 + 도메인 필요
```
cloudflared tunnel login              (브라우저에서 도메인 인증)
cloudflared tunnel create law-dash    (터널 생성)
cloudflared tunnel route dns law-dash dash.내도메인.com
```
그리고 `%USERPROFILE%\.cloudflared\config.yml`:
```yaml
tunnel: law-dash
credentials-file: C:\Users\내계정\.cloudflared\<터널ID>.json
ingress:
  - hostname: dash.내도메인.com
    service: http://localhost:6155
  - service: http_status:404
```
실행: `cloudflared tunnel run law-dash` → 항상 `https://dash.내도메인.com` 로 접속.

### 6-2. 로그인 잠금 (Cloudflare Access) — 강력 추천
Cloudflare 대시보드 → **Zero Trust → Access → Applications** →
`dash.내도메인.com` 등록 → 정책: **"내 이메일만 허용"**.
→ 이제 접속하면 이메일 인증(One-time PIN)을 거쳐야만 열립니다. **개인 노트 보호 필수.**

> 도메인이 없다면: 우선 5절 Quick Tunnel로 쓰되 **주소를 아무에게도 알리지 마세요.** 또는 7절 Tailscale.

---

## 7. 회사컴·폰에서 쓰기 & 운영 팁

- **회사컴**: 로컬 대시보드 **실행하지 말고**, 브라우저로 집컴 주소만 접속. (회사컴에선 아무 것도 설치 안 함)
- **폰**: 브라우저로 접속 → 크롬 메뉴 **"홈 화면에 추가"** 하면 앱처럼 아이콘 생김.
- **집컴은 켜져 있어야** 회사·폰에서 됩니다. (모니터만 꺼져도 OK, 절전/최대절전은 해제 권장)
  - 절전 해제: 설정 → 전원 → "절전 모드 안 함"
- **집 IP가 바뀌어 API가 "불러오기 실패"** 나면 → 법제처 오픈API 신청정보에서 **현재 집 공인 IP로 재등록**.
- **정기 백업**: 가끔 `data\dashboard.db`(회독·노트 전부)를 복사해 두세요.

### 대안 — Tailscale (더 사적, 도메인·공개주소 불필요)
회사컴에 프로그램 설치가 가능하다면 Tailscale이 더 간단·안전할 수 있음(내 기기끼리만 연결, 공개 URL 없음).
단, **회사컴에 설치가 막혀 있으면** Cloudflare Tunnel(브라우저만으로 접속)이 유일한 선택.

---

## 8. 상시 운영을 원하면 (집컴 끄고 싶을 때)

집컴을 24시간 켜두기 싫으면 → **고정 IP를 주는 VPS**(월 몇 천원대)에 대시보드를 올리고, **그 VPS의 고정 IP를 법제처에 등록**. 그러면 집컴/회사컴/폰 모두 항상 접속 가능. (원하면 Claude가 VPS 배포까지 안내)

---

## 9. 문제 해결 (FAQ)

| 증상 | 확인 |
|---|---|
| 터널 주소 접속이 안 됨 | 대시보드 창(6155)이 떠 있나? `http://localhost:6155` 집컴에서 직접 열어보기 |
| 폰에서 "불러오기 실패" | 집컴 공인 IP가 법제처에 등록됐나? OC=`.env의 OPENLAW_OC` 확인 |
| `cloudflared` 명령을 못 찾음 | winget 재설치 or exe를 프로젝트 폴더에 `cloudflared.exe`로 넣기 |
| 회독이 기기마다 다름 | 회사컴에서 **로컬 대시보드를 또 켠 것** — 끄고 집컴 주소로만 접속 |
| 느림 | 집 인터넷 업로드 속도에 의존(정상). VPS면 빨라짐 |

---

## 10. 최종 체크리스트

- [ ] (2) dashboard.db 백업 + 정본 결정
- [ ] (3) WAL 적용 (`journal_mode = wal`) + threaded=True
- [ ] (4) cloudflared 설치
- [ ] (5) `동기화서버_시작.bat` 실행 → 폰에서 주소 접속 성공
- [ ] (5) 회독 +1 → 다른 기기에서 반영 확인 (동기화 OK)
- [ ] (6) (선택) 고정주소 + Cloudflare Access 로그인 잠금
- [ ] (7) 집컴 절전 해제 / 정기 백업

---
문의/막힌 부분은 Claude에게 "인계인수서 O절에서 막혔어" 라고 하면 이어서 도와드립니다.
